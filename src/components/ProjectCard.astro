---
export interface Props {
  title: string;
  scope: string;
  location: string;
  services: string[];
}

const { title, scope, location, services } = Astro.props;
---

<div class="project-card animate-fade-up">
  <div class="project-card-header">
    <h3 class="project-card-title">{title}</h3>
    <span class="project-card-location">{location}</span>
  </div>
  
  <p class="project-card-scope">{scope}</p>
  
  <div class="project-card-services">
    {services.map((service) => (
      <span class="project-card-tag">{service}</span>
    ))}
  </div>
</div>

<style>
  .project-card {
    display: flex;
    flex-direction: column;
    padding: 1.75rem;
    background: #12161B;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1rem;
    transition: all 0.3s ease;
    height: 100%;
  }
  
  .project-card:hover {
    border-color: rgba(30, 200, 232, 0.3);
    transform: translateY(-2px);
  }
  
  .project-card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.75rem;
  }
  
  .project-card-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #fff;
    transition: color 0.2s ease;
  }
  
  .project-card:hover .project-card-title {
    color: #1EC8E8;
  }
  
  .project-card-location {
    flex-shrink: 0;
    padding: 0.375rem 0.75rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: #1EC8E8;
    background: rgba(30, 200, 232, 0.1);
    border-radius: 9999px;
  }
  
  .project-card-scope {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: #6B7280;
    margin-bottom: 1.25rem;
    flex: 1;
  }
  
  .project-card-services {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: auto;
  }
  
  .project-card-tag {
    padding: 0.25rem 0.625rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: #9CA3AF;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 0.375rem;
  }
</style>
